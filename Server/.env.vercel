# Vercel Environment Variables Template
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Required for all environments (Production, Preview, Development)

# Database (Get from Neon, Supabase, or other PostgreSQL provider)
DB_URL=postgresql://neondb_owner:npg_8ogDZIa5TijF@ep-cold-wildflower-adoir18w-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require

# Security
NODE_ENV=production
BCRYPT_SALT_ROUNDS=12

# JWT Configuration (Generate secure random strings)
# Generate with: openssl rand -base64 32
JWT_ACCESS_SECRET=
JWT_ACCESS_EXPIRES_IN=7d
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=1y

# Admin Account
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_PROFILE_PHOTO=
ADMIN_MOBILE_NUMBER=

# Cloudinary (Image Upload)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email Configuration
SENDER_EMAIL=
SENDER_APP_PASS=

# Frontend URL (Your deployed Next.js app)
CLIENT_URL=https://your-frontend.vercel.app

# Payment (if using)
PAYMENT_URL=
STORE_ID=
SIGNATURE_KEY=
PAYMENT_VERIFY_URL=

# SMS (Twilio - if using)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Notes:
# - Don't use quotes around values in Vercel
# - Generate strong secrets using: openssl rand -base64 32
# - For DATABASE_URL, ensure you append ?sslmode=require for SSL connections
# - Update CLIENT_URL after deploying your frontend
